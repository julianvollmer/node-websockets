/**
 * Creates an (client) websocket upgrade request.
 * 
 * This function is used by the WebSocketClient to create and send a WebSocket
 * upgrade request to the server.
 */
function createUpgradeRequest() {

}

/**
 * Handles an (client) websocket upgrade request.
 * 
 * This function is used by the WebSocketServer to handle (analyse) a requested upgrade.
 */
function handleUpgradeRequest() {

}

/**
 * Creates an (server) websocket upgrade response.
 * 
 * This function is used by the WebSocketServer to create a response out of the upgrade request.
 */
function createUpgradeResponse() {
    
}

/**
 * Handles an (server) websocket upgrade response.
 * 
 * This function is used by the WebSocketClient to handle (analyse) the response of the WebSocketServer.
 */
function handleUpgradeResponse() {

}

exports.createUpgradeRequest = createUpgradeRequest;
exports.handleUpgradeRequest = handleUpgradeRequest;
exports.createUpgradeResponse = createUpgradeResponse;
exports.handleUpgradeResponse = handleUpgradeResponse;